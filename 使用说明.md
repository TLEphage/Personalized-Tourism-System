前端使用npm install 安装vue所需要的依赖
再使用npm install <package> 安装FrontEnd/requirements.txt中的依赖,package为requirements.txt中的包
启动命令为: npm run dev
后端使用 pip install -r requirements.txt安装所需要的依赖（可使用虚拟环境）
启动命令为 uvicorn app.main:app --reload